// src/components/Cart.tsx
import React from 'react';

export default function Cart() {
  return (
    <div>
      <h2>Carrito</h2>
      {/* Lista de productos, precios, botones, etc */}
    </div>
  );
}
